apiVersion: workflows.tekton.dev/v1alpha1
kind: Workflow
metadata:
  name: example-workflow
spec:
  pipelineSpec:
    tasks:
      - name: echo-hello
        taskSpec:
          steps:
          - image: busybox
            script: echo "hello"
  params:
    - name: default-repo
      default: my-repo
